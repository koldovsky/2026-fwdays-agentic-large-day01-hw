# Repo Instructions

- At the start of every task, read all Markdown files in `docs/memory-bank/`.
- Treat `docs/memory-bank/` as the primary home for durable project context; keep `AGENTS.md` focused on repo operating rules.
- Use the `memory-bank-manager` skill for Memory Bank initialization, updates, audits, or de-duplication work.
- When documenting risky source behavior, prefer `docs/technical/hidden-invariants.md`; keep Memory Bank to compact summaries and links.
